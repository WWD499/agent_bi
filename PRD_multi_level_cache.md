# PRD：agent_bi「真·多级缓存」

> 文档类型：简单 PRD（新功能需求，非 Bug 修复）｜语言：简体中文
> 项目：agent_bi（基于 RuoYi 的 AI 数据分析平台）
> 后端栈：Spring Boot 3.4.5 + Java 21 + Maven，已集成 Redis；计划引入 Caffeine（依赖引入待确认）
> 原始需求复述：用户原话"做真·多级缓存"——建立**真正的 L1 本地 + L2 Redis 两级缓存架构**，替代当前互相隔离的多套单级缓存（DataProbe 的进程内裸 `ConcurrentHashMap`、AgentMemory 的纯 Redis 三件套、LlmService 无缓存）。

---

## 1. 产品目标

| 编号 | 目标 | 说明（基于已核实现状） |
|------|------|------------------------|
| G1 | **统一缓存抽象** | 用一套可复用的 L1（本地）+ L2（Redis）两级缓存组件，替代当前"三套互相隔离单级方案"的现状：① `DataProbeService` 的进程内裸 `ConcurrentHashMap`；② `AgentMemory` 的纯 Redis（List/ZSet/Hash）；③ `LlmService` 无缓存直连网关。 |
| G2 | **解决可靠性与成本痛点** | 消除 `DataProbeService` 进程内缓存在**进程重启即丢失、多实例不共享**的问题；为 `LlmService` 补齐代码注释中"Phase 2（Spring AI + Redis 记忆）一并补齐"的缓存预留项，降低重复 LLM 调用带来的 **token 浪费与端到端延迟**。 |
| G3 | **跨实例一致、可演进** | 缓存统一命名、统一 TTL、统一失效，保证多实例下行为一致；预留可观测（命中率/统计）与策略可调能力，支撑后续把更多热点（如 AgentMemory 热会话）纳入同一框架，而非再写一套。 |

**期望达到的效果**：统一缓存抽象、组件可复用、跨实例一致、可观测、易扩展。

---

## 2. 用户故事

| 编号 | 角色 | 用户故事 |
|------|------|----------|
| US-1 | 平台开发者 | 作为平台开发者，我希望有一个统一的缓存组件，而不是每个服务各写一套（裸 `ConcurrentHashMap` / 裸 Redis），以便在新增缓存场景时复用、降低重复代码与一致性风险。 |
| US-2 | 运维人员 | 作为运维人员，我希望缓存能跨多实例共享且重启不丢，以便在水平扩容或重启发布时，探查结果等缓存不失效、各实例表现一致。 |
| US-3 | 终端用户（对话/查询） | 作为终端用户，我希望重复问相似问题、或对同一张表做探查时能更快返回，以便减少等待时间，并降低重复计算的资源消耗。 |
| US-4 | 平台开发者 | 作为平台开发者，我希望 LLM 的重复调用能被缓存（补齐 Phase 2 预留），以便在 NL2SQL 与对话链路中减少 token 成本与网关延迟。 |
| US-5 | 运维人员 | 作为运维人员，我希望能看到缓存命中率等可观测指标，以便评估缓存收益、定位"缓存没生效/命中低"的问题。 |

---

## 3. 需求池（P0 / P1 / P2）

> 优先级定义：**P0 必须（Must）**｜**P1 重要（Should）**｜**P2 可选/后续（Nice to have）**
> 说明：本表只定义"需求侧"（做什么、验收什么），不含代码与架构实现细节（由架构师设计）。

### 概览

| 编号 | 需求 | 优先级 |
|------|------|--------|
| P0-1 | 建立可复用的两级缓存抽象组件（L1 本地 + L2 Redis，read-through + 写回/写穿，TTL 对齐，一致失效） | P0 |
| P0-2 | 将 `DataProbeService` 的裸 `ConcurrentHashMap` 迁移到该组件（解决重启丢失 + 多实例共享） | P0 |
| P0-3 | 为 `LlmService` 的 LLM 响应接入该组件做缓存（完成 Phase 2 预留项） | P0 |
| P1-1 | 缓存命中率/统计可观测（至少日志，建议 metrics） | P1 |
| P1-2 | 缓存 key 命名规范统一 | P1 |
| P1-3 | 支持手动失效（clear）接口 | P1 |
| P2-1 | 将 `AgentMemory` 热会话读取也加 L1 本地层（目前纯 Redis 单级） | P2 |
| P2-2 | 缓存压缩 / 淘汰策略可调 | P2 |

### P0（必须）

#### P0-1 两级缓存抽象组件
- **优先级**：P0（必须）
- **描述**：提供一个可复用的两级缓存抽象能力，对外屏蔽 L1（本地）与 L2（Redis 远程）的差异。需支持：**read-through（读穿透：先查 L1，未命中查 L2，再未命中回源并回填两级）**、**写回/写穿（write-through / write-behind，策略待确认）**、**L1 与 L2 的 TTL 可配置且对齐**、**统一的失效（invalidate）机制保证两级一致性**。该组件需能被多个业务服务复用（`DataProbeService`、`LlmService` 等共用）。
- **验收标准（AC）**：
  - AC1：提供统一的两级缓存接入点，业务方无需自行编排 L1/L2 查询与回填顺序。
  - AC2：read-through 行为正确——L1 命中直接返回；L1 未命中查 L2；L2 未命中回源，并将结果同时写回 L1 与 L2。
  - AC3：写入时 L1 与 L2 保持一致（具体写穿/写回策略以"待确认问题 Q1"结论为准）。
  - AC4：L1、L2 的 TTL 可经配置设置并对齐；过期由组件统一处理，业务方无需手算过期时间戳。
  - AC5：提供一致性失效能力，失效后 L1 与 L2 均不再返回旧值。
  - AC6：至少 `DataProbeService` 与 `LlmService` 两处能基于该组件接入（见 P0-2、P0-3），证明可复用。

#### P0-2 `DataProbeService` 裸 Map 迁移
- **优先级**：P0（必须）
- **描述**：将 `DataProbeService` 当前进程内裸 `ConcurrentHashMap`（key = 数据源ID:表名，value 带过期时间，`ProbeConstants.CACHE_TTL_MINUTES = 10` 分钟）迁移到 P0-1 的两级缓存组件，解决"进程重启即丢、多实例不共享"两个问题。
- **验收标准（AC）**：
  - AC1：移除 `DataProbeService` 内 `ConcurrentHashMap` 自管 TTL 的逻辑，改走两级缓存组件。
  - AC2：key 维度保持 `数据源ID:表名` 不变，探查命中行为与现状一致（命中则省去一次只读探查）。
  - AC3：**进程重启后缓存不丢失**（依赖 L2 Redis 持久化），验证了"重启即丢"痛点消除。
  - AC4：**多实例共享同一份缓存**（任一组网实例写入，其它实例可读到），验证了"多实例不共享"痛点消除。
  - AC5：TTL 仍按 `ProbeConstants.CACHE_TTL_MINUTES`（默认 10 分钟，可由 `bi.probe.*` 配置覆盖）生效。
  - AC6：现有降级语义保持不变（探查整体异常仍返回部分/空结果，不阻断用户查询）。

#### P0-3 `LlmService` 响应接入缓存
- **优先级**：P0（必须）
- **描述**：为 `LlmService` 的 LLM 调用响应接入 P0-1 两级缓存组件，完成其在代码注释中预留的"Phase 2（Spring AI + Redis 记忆）一并补齐"的缓存能力，避免相同请求重复直连网关、浪费 token 与延迟。
- **验收标准（AC）**：
  - AC1：`LlmService` 中 `chat` / `chatWithHistory` 等响应可经两级缓存组件缓存（具体缓存哪些入口、key 维度与 TTL 以"待确认问题 Q2"为准）。
  - AC2：相同请求（如相同 model + prompt 哈希）命中缓存时返回缓存响应，不再发起网关调用（可通过日志或计数验证网关调用次数下降）。
  - AC3：接入缓存后**不破坏现有异常透传与降级逻辑**（401/403/429 等业务异常仍按现状抛出/处理）。
  - AC4：缓存不存在时端到端行为与现状完全一致（只是多了一次缓存填充）。

### P1（重要）

#### P1-1 缓存命中率 / 统计可观测
- **优先级**：P1（重要）
- **描述**：两级缓存组件需具备命中率与基础统计的可观测能力；最低要求是日志（INFO 级周期性打印命中/未命中与命中率），建议同时暴露 metrics（如 Micrometer）。
- **验收标准（AC）**：
  - AC1：能统计命中次数、未命中次数与命中率（至少按组件/命名空间维度）。
  - AC2：至少提供日志输出（周期性打印命中率），便于无监控平台也能观察。
  - AC3（建议）：暴露 metrics 端点/指标，可被现有监控采集（非强验收，若本期未做则列入后续）。

#### P1-2 缓存 key 命名规范统一
- **优先级**：P1（重要）
- **描述**：制定并落地统一的缓存 key 命名规范，使各业务接入（DataProbe、LlmService、后续 AgentMemory 等）遵循一致约定；L1 与 L2 的同一逻辑缓存应使用一致的 key 映射。
- **验收标准（AC）**：
  - AC1：产出统一的 key 命名规范（含前缀、分隔符、命名空间约定），各接入点遵循。
  - AC2：同一逻辑缓存在 L1 与 L2 使用一致的逻辑 key（避免两套 key 体系导致无法统一失效/观测）。
  - AC3：规范覆盖 Redis 侧的命名空间隔离，避免与现有 `bi:agent:*`、`SaToken` 等 key 冲突。

#### P1-3 手动失效（clear）接口
- **优先级**：P1（重要）
- **描述**：两级缓存组件对外提供手动失效接口（如按 key 失效、按命名空间批量失效），且失效需同时作用于 L1 与 L2。
- **验收标准（AC）**：
  - AC1：提供 `clear(key)` / `clearAll(namespace)`（或等价）手动失效接口。
  - AC2：调用失效后，L1 与 L2 均不再返回该旧值。
  - AC3：失效操作在多实例下有效（至少保证 L2 失效；L1 跨实例失效策略见 Q6）。

### P2（可选 / 后续）

#### P2-1 `AgentMemory` 热会话加 L1 本地层
- **优先级**：P2（可选）
- **描述**：为 `AgentMemory` 当前纯 Redis 单级方案的高频读取（如当前活跃会话的会话详情/历史）增加 L1 本地层，加速热路径。注意其结构是 List/ZSet/Hash 三件套（非纯 KV），是否纳入 KV 抽象见 Q3。
- **验收标准（AC）**：
  - AC1：热会话高频读取（如 `get` / `getSession`）经 L1 本地层加速，降低 Redis 往返。
  - AC2：与现有 Redis 三件套结构兼容，写入/失效后 L1 与 Redis 保持一致。

#### P2-2 缓存压缩 / 淘汰策略可调
- **优先级**：P2（可选）
- **描述**：L1 的淘汰策略（LRU / LFU / 基于大小或权重）可配置；可选支持大 value 压缩存储，降低内存占用。
- **验收标准（AC）**：
  - AC1：L1 淘汰策略（LRU/LFU/基于大小等）可经配置调整，并有合理默认。
  - AC2（可选）：大 value 支持压缩存储，且不影响读写正确性。

---

## 4. 待确认问题（需用户 / 架构师拍板）

| 编号 | 开放问题 | 为什么重要 |
|------|----------|------------|
| Q1 | 两级缓存一致性策略选 **write-through（写穿）** 还是 **write-behind（写回）**？还是仅 **read-through + 主动失效**（最简单、最强一致）？ | 直接决定 P0-1 的写入语义、复杂度与一致性强度。 |
| Q2 | **LLM 响应缓存的 key 维度**：按 `model + prompt` 哈希？是否区分用户/租户？TTL 多长合适（如 1h / 24h）？流式（`streamChat`）与带 `tool_calls` 的原生调用（`chatRaw`）是否也缓存？ | 决定 P0-3 的命中范围、隔离性与时效性；key 设计错误会导致串用户或缓存失效过快。 |
| Q3 | **AgentMemory 是否纳入多级框架**？其结构是 List/ZSet/Hash 三件套（非纯 KV），是强行套进 KV 抽象，还是保持独立、仅给热读取加 L1（P2-1）？ | 影响 P2-1 范围与整体架构整洁度。 |
| Q4 | **Caffeine 依赖是否允许引入**？项目当前未引入，需评估体积 / 许可 / 与 Spring Boot 3.4.5 + Java 21 的兼容性。 | 决定 L1 选型，是 P0-1 的前置依赖。 |
| Q5 | **L2 是否复用现有 Redis 实例**（与 `SaToken` / `AgentMemory` 同库）？key 命名空间如何隔离、容量与 Redis 侧淘汰策略（`maxmemory-policy`）如何评估？ | 影响运维边界、容量与潜在相互影响。 |
| Q6 | **多实例下 L1 失效如何广播**？L2 失效后如何通知其它实例的 L1 失效（如 Redis Pub/Sub），还是接受短暂不一致、仅依赖 TTL 兜底？ | 决定跨实例一致性强度与实现成本（影响 P1-3 的 AC3）。 |

---

## 本期范围建议（总结）

**本期建议范围：P0 全部必做 + 三个 P1 全部纳入本期。**
P0 是这次需求的骨架——先建好可复用的两级缓存抽象组件（P0-1），并把现状里两个最痛的点直接消化掉：`DataProbeService` 裸 Map 迁移（P0-2，解决重启丢失与多实例不共享）和 `LlmService` 接入缓存（P0-3，补齐 Phase 2 预留、省 token/延迟）。三个 P1（命中率可观测 P1-1、key 命名规范 P1-2、手动失效 P1-3）成本低、却是"真·多级缓存"能不能用、能不能管的基准线——没有可观测和统一命名/失效，多级缓存只是又多了一套黑盒，强烈建议本期一并落地。P2（AgentMemory 热会话 L1、压缩/淘汰可调）建议放到下一期，等 P0/P1 在 DataProbe 与 LlmService 上跑稳、Q1–Q4 结论清晰后再推广，避免一开始就把结构特殊的 AgentMemory 强行套进 KV 抽象而增加风险。
